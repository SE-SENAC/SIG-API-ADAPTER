name: Deploy SIG API Adapter

on:
  push:
    branches:
      - Production

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Executar deploy
        run: bash /srv/sig-api-adapter/deploy.sh